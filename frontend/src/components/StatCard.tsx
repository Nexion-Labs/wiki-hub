import { memo } from 'react';
import { getStatCardBgClass, getStatCardIconClass, type StatCardColor } from '../lib/theme';

/**
 * StatCard component for dashboard statistics
 * Following Vercel React Best Practices:
 * - rerender-memo: Memoized component
 * - rendering-hoist-jsx: SVG icons hoisted outside component
 * - rerender-dependencies: Primitive props only
 */

interface StatCardProps {
  title: string;
  value: number;
  color: StatCardColor;
  icon: React.ReactNode;
}

// Hoist static SVG icons outside component (rendering-hoist-jsx)
export const Icons = {
  Document: (
    <svg
      className="w-8 h-8"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
      />
    </svg>
  ),
  Package: (
    <svg
      className="w-8 h-8"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"
      />
    </svg>
  ),
  Users: (
    <svg
      className="w-8 h-8"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"
      />
    </svg>
  ),
  Alert: (
    <svg
      className="w-8 h-8"
      fill="none"
      stroke="currentColor"
      viewBox="0 0 24 24"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
      />
    </svg>
  ),
} as const;

export const StatCard = memo<StatCardProps>(({ title, value, color, icon }) => {
  // Get color classes from memoized Map lookups
  const bgClass = getStatCardBgClass(color);
  const iconColorClass = getStatCardIconClass(color);

  return (
    <div className="bg-white rounded-lg shadow p-6">
      <div className="flex items-center justify-between">
        <div>
          <p className="text-sm font-medium text-gray-600">{title}</p>
          <p className="text-3xl font-bold text-gray-900 mt-2">{value}</p>
        </div>
        <div className={`p-3 ${bgClass} rounded-full`}>
          <div className={iconColorClass}>{icon}</div>
        </div>
      </div>
    </div>
  );
});

StatCard.displayName = 'StatCard';
